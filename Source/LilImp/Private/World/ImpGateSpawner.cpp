// Jassar alotaibi | Weaver of worlds | all rights reserved.
// spawns gates, randomizes gap, pacing

#include "World/ImpGateSpawner.h"

// Sets default values
AImpGateSpawner::AImpGateSpawner()
{
 	// Set this actor to call Tick() every frame.  You can turn this off to improve performance if you don't need it.
	PrimaryActorTick.bCanEverTick = true;

}

// Called when the game starts or when spawned
void AImpGateSpawner::BeginPlay()
{
	Super::BeginPlay();
	
}

// Called every frame
void AImpGateSpawner::Tick(float DeltaTime)
{
	Super::Tick(DeltaTime);

}

